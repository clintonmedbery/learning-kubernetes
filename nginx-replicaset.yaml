apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-replicaset
  labels:
    name: front-end-app
spec:
  replicas: 3
  selector:
    matchLabels:
      env: production
  template:
    metadata:
      name: front-nginx-1
      labels:
       env: production
    spec:
      containers: 
        - name: nginx
          image: nginx